{
  "name": "ds-css-reset",
  "version": "1.0.0",
  "description": "A modern CSS reset using CSS layers",
  "main": "dist/reset.css",
  "scripts": {
    "build": "mkdir -p dist && cp src/reset.css dist/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"serve .\" \"open-cli http://localhost:3000/demo/index.html\""
  },
  "keywords": [
    "css",
    "reset",
    "css-layers",
    "modern-css"
  ],
  "author": "",
  "license": "MIT",
  "files": [
    "dist",
    "README.md"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "open-cli": "^8.0.0",
    "serve": "^14.2.4"
  }
}
